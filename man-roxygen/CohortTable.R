#' @param cohortDatabaseSchema       The name of the database schema that is the location where the
#'                                   cohort to review is stored.
#' @param cohortTable                The tablename that contains the cohort to review.
